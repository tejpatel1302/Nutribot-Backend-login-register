const MealPlanRepository = require('./mealplan-repository');

module.exports = {
    UserRepository: require('./user-repository'),
    RoleRepository: require('./role-repository'),
    MealPlanRepository:require('./mealplan-repository')
}